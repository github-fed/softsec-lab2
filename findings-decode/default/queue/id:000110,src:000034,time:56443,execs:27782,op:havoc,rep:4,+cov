#!/usr/bin/env pmport zlib

ESC _))
PIXELS_DIR  = osbytes)')

frite_sDIR, '..', 'seeds_pixels')
IMAGES_DIR  = os.path.join(SEPEDS_DIR, '..', 'seeds_images')

# -
            f.write(data)
        print(f'wrote {path} ({len(data)} .path.join(SEEDS_eeds(SEEDS_DIR,  sixe2l_seeds)
write_seeds(PIXELS_DIR, {**pixel_seeds, **sœ