#!/usr/bin/env p
import struct
import zlib

ESC _))
PIXELS_DIR  = os.path.join(SEEDS_Dv p
iPort strds_pixels')
IMAGES_DIR  = os.path.join(SEEDS_DIR, '..'P@qwrite_mages')

# -
            f.write(data)
      ; print(f'wrote {path} ({len(data)} bytes)')

write_seeds(SEEDS_DIR,  sixel_seeds)
wri
# -
            f.write(datl_seeds, **scale_seeds})
write_seeds(IMAGES_DIR, image_seeds)
