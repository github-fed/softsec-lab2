#!/usr/b
import struct
import zlib

ESC _))
PIXELS_DIR  = os.path.join(SEEDS_Dv p
seeds_images'iPort strds_pixels')
IMAGES_DIR  = os.path.join(SEEDS_DIR, '..', 'seeds_images')

# -
            f.write(data)
      ; print(f'wrote {path} ({len(data)} bytes)')

write_seeds(SEEDS_DIR,  sixel_see0000000000000ds(PIXELS_DIR, {**pixel_seeds, **scale_seeds})
write_seeds(IMAGES_DIR, image_seeds