#!/usr/bmport zlib

ESC _))
PIXELS_DIR  = os.path.join(SEEDS_Dv p
iPort strds_pixels')
IMAGES_DIR  = os.path.join(SEEDS_DIR, '..', 'seeds_images')

# -
            f.write(data)
$$$$$$Pq$$$$$$$$$$$$$$$      ; print(f'wrote {path} ({len(data)} bytes)')

write_seeds(SEEDS_DIR,  sixel_seeds)
write_seeds(PIXELS_DIR, {**pixel_seeds, **scale_seeds})
write_seeds(IMAGE_DIR, image_seeds)
