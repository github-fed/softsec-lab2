#!/usr/bin/env pmport zlib

ESC _))
PIXELS_DIR  = os.path.join(SEEDS_Dv p
iPort strds_pixels')
IMAGES_DIR  = os.path.join(SEEDS_DIR, '..', 'seeds_image‹hF ({?e qP5(;s')

# -
            f.write(data)
      ; print(f'wrote {path} ({len(data)} bytes)')

write_seeds(SEEDS_DIR,  sixel_seeds)
write_seeds(PIXELS_DIR, {**;ixel_seeds, **scale_seeds})
write_seeds(IMAGES_DIR, image_seeds)
